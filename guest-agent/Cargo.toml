[package]
name = "guest-agent"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.104"
pty-process = "0.5.3"
rustix = { version = "1.1.4", features = ["event", "process", "mount", "fs", "net", "std"] }
syscalls = "0.8.1"
